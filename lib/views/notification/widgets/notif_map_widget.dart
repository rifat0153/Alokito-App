import 'package:flutter/material.dart';

class NotifMapWidget extends StatelessWidget {
  const NotifMapWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
